{% extends 'baseAdmin.html.twig' %}


{% block content %}
<div class="content-wrapper">
<div class="card">
                <div class="card-header">
                    <h3 class="card-title">Questions</h3>
                   
                    <div class="card-tools">
                        <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                            <i class="fas fa-minus"></i>
                        </button>
                       
                    </div>
                </div>
                 <a class="btn btn-info btn-sm" href={{path("new_question")}}>
                                    <i class="fas fa-plus-circle">
                                    </i>
                                    Add  Qestion
                                </a>
                <div class="card-body p-0">
                    <table class="table table-striped projects">
                        <thead>
                        <tr>
                            <th style="width: 1%">
                                #
                            </th>
                            <th style="width: 20%">
                               enoncé
                            </th>
                            <th style="width: 30%">
                                Reponse A
                            </th>
                            <th>
                                Reponse B
                            </th>
                            <th style="width: 8%" class="text-center">
                               Reponse correcte
                            </th>
                            <th style="width: 20%">
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        {%for question in questions%}
                        <tr>
                            <td>
                                {{question.getId()}}
                            </td>
                            <td>
                            {{question.getEnonce()}}
                            </td>
                            <td>
                               {{question.getPropositionA()}}

                            </td>
                            <td>
                                  {{question.getPropositionB()}}

                            </td>
                            <td class="project-state">
                                
                            {{question.getPropositionCorrecte()}}
                            </td>
                            <td class="project-actions text-right">
                                
                                <a class="btn btn-info btn-sm" href="{{path("edit_question",{id:question.getId()})}}">
                                    <i class="fas fa-pencil-alt">
                                    </i>
                                    modifier
                                </a>
                                <form action="{{path("delete_question",{id:question.getId()})}}" method="delete">
                                <button class="btn btn-danger btn-sm"> 
                               <i class="fas fa-trash-alt"></i>
                                supprimer</button>
                                </form>
                                 
                            </td>
                        </tr>
                        {%endfor%}
                       
                       
                        </tbody>
                    </table>
                </div>
      </div>
{% endblock %}
