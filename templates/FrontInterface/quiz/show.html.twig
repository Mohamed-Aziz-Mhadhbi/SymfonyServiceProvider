{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block content %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quiz.name }}</td>
            </tr>
            <tr>
                <th>Total_question</th>
                <td>{{ quiz.totalQuestion }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ quiz.difficulty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quiz_index_front') }}">back to list</a>

    <a href="{{ path('quiz_edit_front', {'id': quiz.id}) }}">edit</a>

    {{ include('FrontInterface/quiz/_delete_form.html.twig') }}
{% endblock %}
