{{ form_start(form) }}

    <div class="form-group">
        {{ form_row(form.title) }}
    </div>
    <div class="form-group">
        {{ form_row(form.description) }}
    </div>
    <button class="btn btn-block btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
