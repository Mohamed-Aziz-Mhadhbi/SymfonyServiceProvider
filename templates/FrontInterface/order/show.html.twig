{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ order.message }}</td>
            </tr>
            <tr>
                <th>CreatAt</th>
                <td>{{ order.creatAt ? order.creatAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ order.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_index_front') }}">back to list</a>

    <a href="{{ path('order_edit_front', {'id': order.id}) }}">edit</a>

    {{ include('FrontInterface/order/_delete_form.html.twig') }}
{% endblock %}
