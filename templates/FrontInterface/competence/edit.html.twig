{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Competence{% endblock %}

{% block content %}
<div class="content-wrapper">
    <div class="container-fluid">
        <div class="row">
            <!-- left column -->
            <div class="col-md-6 ">
                <!-- general form elements -->

                <div class="card card-primary">
                    <div class="card-header">
                        <h2 class="card-title">Update Skill</h2>
                    </div>
                    <!-- /.card-header -->
                    <!-- form start -->
                    <div class="card-body">
    {{ include('FrontInterface/competence/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('competence_index') }}">back to list</a>

    {{ include('FrontInterface/competence/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
