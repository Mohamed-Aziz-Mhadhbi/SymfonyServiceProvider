{% extends 'baseAdmin.html.twig' %}

{% block title %}Offre{% endblock %}

{% block content %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ offre.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.description }}</td>
            </tr>
            <tr>
                <th>CreatAt</th>
                <td>{{ offre.creatAt ? offre.creatAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_index_back') }}">back to list</a>



    {{ include('BackInterface/offre/_delete_form.html.twig') }}
{% endblock %}
