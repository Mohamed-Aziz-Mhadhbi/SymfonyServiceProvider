{% extends 'baseAdmin.html.twig' %}

{% block title %}Domain{% endblock %}

{% block content %}
    <h1>Domain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domain.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ domain.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('domain_index_back') }}">back to list</a>

    <a href="{{ path('domain_edit_back', {'id': domain.id}) }}">edit</a>

    {{ include('BackInterface/domain/_delete_form.html.twig') }}
{% endblock %}
