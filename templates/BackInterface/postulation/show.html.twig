{% extends 'baseAdmin.html.twig' %}

{% block title %}Postulation{% endblock %}

{% block content %}
    <h1>Postulation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postulation.id }}</td>
            </tr>
            <tr>
                <th>Motivation</th>
                <td>{{ postulation.motivation }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ postulation.price }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ postulation.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('postulation_index_back') }}">back to list</a>



    {{ include('BackInterface/postulation/_delete_form.html.twig') }}
{% endblock %}
