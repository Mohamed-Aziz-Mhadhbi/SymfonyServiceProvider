{{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
    <div class="row">
        <div class="col-sm-6">
            <!-- text input -->
            <div class="form-group">
                {{ form_row(form.titre) }}
            </div>
        </div>
        <div class="col-sm-6">
            <div class="form-group">
                {{ form_row(form.categorie) }}
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <button class="btn btn-block btn-success btn-sm">{{ button_label|default('Add') }}</button>
        </div>
    </div>
{{ form_end(form) }}
